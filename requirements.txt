# Minimal requirements for the char-level transformer notebook
# Adjust torch version depending on your platform (MPS/CUDA). See README.md for notes.
torch>=2.0.0
numpy>=1.24
tqdm>=4.65
jupyterlab
ipykernel
# Optional (use if you need visualization or dataset helpers)
# matplotlib
# datasets
